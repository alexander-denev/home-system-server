name: nextcloud
services:
    nextcloud:
        image: nextcloud:latest
        restart: unless-stopped
        volumes:
            - ${EXTERNAL_MOUNTPOINT}:/var/www/html
        ports:
            - 80:80 # Web interface

networks:
    default:
        name: dabase
        external: true

volumes:
    nextcloud_data:
